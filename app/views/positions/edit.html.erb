<% provide(:button_text, 'Update Position') %>

<div class="row justify-content-md-center">
<div class="col-md-8">
  <h3><%= @portfolio.name %> : <%= @position.symbol %> - Edit Position</h3>
<div class="card p-3">
  <% if @portfolio.cashonly? %>
    <%= render 'cash_form' %>
  <% else %>
    <%= render 'form' %>
  <% end %>
</div>
</div>
</div>
