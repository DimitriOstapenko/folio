<% user = current_user %>

<%# line_chart CURRENCIES, xtitle: "Currency", ytitle: "Value"  %>
<%# area_chart CURRENCIES  %>
<%# column_chart CURRENCIES %>

<%# column_chart PortfolioHistory.where(user_id: user.id).reorder('').group('date(created_at)').sum("curval*fx_rate"), %>
<%= area_chart PortfolioHistory.where(user_id: user.id).reorder('').group('date(created_at)').sum("curval*fx_rate"),
#  points: false,
  prefix: "$",
  thousands: ",",
  messages: {empty: "No data"},
  min: 3500000,
  max: 4500000,
#  width: "1000px",
  height: "800px",
#  xtitle: "Time", 
#  ytitle: "Current Value (CAD)",
  title: "Net Worth",
  download: {filename: "NetWorth"}
%>

