<% provide(:button_text, 'Update Transaction') %>

<div class="row justify-content-md-center">
<div class="col-md-8">
  <h5><%= @portfolio.name %>: <%= @position.symbol %> - Edit Transaction</h5>
  <div class="card p-3 bg-light">
  <div class="card-body">

  <% if @position.is_cash? %>
    <%= render 'cash_form' %>
  <% else %>
    <%= render 'form' %>
  <% end %>

  </div>
</div>
</div>
</div>
